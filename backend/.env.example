# Backend EAD CBMAM — variáveis de ambiente
# Copie este arquivo para .env e preencha os valores reais. NÃO comite o .env.

# === Banco MySQL ===
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=cilomi
DB_PASSWORD=
DB_NAME=cursos

# === Segurança ===
# Gere algo com pelo menos 32 caracteres, ex.: openssl rand -hex 32
JWT_SECRET=

# === Servidor ===
PORT=5000
